The dataaccess classes are the last layer of our project and directly connected to the relational database's tables. Hibernate framework handled the simple CRUD operations but
We had to add queries for any further action needed.
